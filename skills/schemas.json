[
  {
    "name": "embed",
    "title": "embed",
    "description": "Generate embeddings for one or more input strings using the configured embeddings provider. Returns an OpenAI-compatible response: `{ object: \"list\", data: [{ object, index, embedding }], model, usage }`.",
    "routePath": "embeddings/embed",
    "parameters": {
      "type": "object",
      "properties": {
        "input": {
          "type": [
            "string",
            "array"
          ],
          "description": "Input text to embed — a single string or an array of strings for batch embedding"
        },
        "model": {
          "type": "string",
          "description": "Model name echoed back in the response. The active embedding model is determined by the configured provider."
        }
      },
      "required": [
        "input"
      ]
    }
  },
  {
    "name": "models",
    "title": "models",
    "description": "Router for models",
    "routes": [
      {
        "name": "enconvo_ai",
        "description": "",
        "parameters": {
          "type": "object",
          "properties": {},
          "required": []
        },
        "routePath": "embeddings/models/enconvo_ai"
      },
      {
        "name": "ollama",
        "description": "",
        "parameters": {
          "type": "object",
          "properties": {},
          "required": []
        },
        "routePath": "embeddings/models/ollama"
      },
      {
        "name": "open_ai",
        "description": "",
        "parameters": {
          "type": "object",
          "properties": {},
          "required": []
        },
        "routePath": "embeddings/models/open_ai"
      },
      {
        "name": "siliconflow",
        "description": "",
        "parameters": {
          "type": "object",
          "properties": {},
          "required": []
        },
        "routePath": "embeddings/models/siliconflow"
      },
      {
        "name": "voyage",
        "description": "",
        "parameters": {
          "type": "object",
          "properties": {},
          "required": []
        },
        "routePath": "embeddings/models/voyage"
      }
    ]
  }
]
